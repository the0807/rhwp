# 2026-05-21 — 오늘 할일

## M100 — PR 처리 (메인테이너)

### PR #1026 — fix(text_measurement): 좁은 구두점 (U+2018/U+2019/U+2027) 폭 분류 + native/WASM 동기화 (재제출, 외부 기여 @HaimLee-4869, Refs PR #1021)

- [x] PR 정보 + 사이클 점검 (@HaimLee-4869 4번째 기여, 1차 5/20 옵션 B 보류 후 KTX 회귀 가드 + rebase 후 재제출)
- [x] 이전 검토 결과 확인 (`mydocs/pr/archives/pr_1026_review.md` + `pr_1026_report.md`) — 1차 보류 사유: KTX 페이지번호 +10px 회귀
- [x] 재제출 변경 사항 분석 (PR 본문 "C8+C9 소스 로직 변경 없음" — base rebase + KTX 회귀 가드 + golden 갱신만)
- [x] devel 동기화 (`ac6aeed4`) — PR base = `bbd38e85` (PR #1033 머지 후), origin/devel = `ac6aeed4` (PR #1034 머지 후)
- [x] 코드 본질 분석 — text_measurement.rs +26/-2 (1차와 동일 H1+H2+H3) + tests/issue_874_ktx_toc_page_number_right_align.rs +98 (신규 KTX 가드) + golden 갱신 issue-157/issue-617 (issue-267 KTX 보존)
- [x] 재검토 문서 `pr_1026_review_v2.md` → 작업지시자 옵션 A 승인 (1차 회귀 정정 + 회귀 가드 영구화)
- [x] cherry-pick `cb53dd94` (author @HaimLee-4869 보존, 충돌 없음)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** + 통합 모두 passed + cargo test --test issue_874_ktx_toc_page_number_right_align **1 passed** (KTX 페이지번호 정합 정량 입증, max_x < 695.0 단언) + cargo test --test svg_snapshot **8/8 passed** (issue_267_ktx_toc_page 포함, KTX golden 보존) + fmt --all clean
- [x] sweep 7 fixtures (BEFORE `ac6aeed4` ↔ AFTER): **32 diff = 모두 본 PR 본질**:
  - KTX 9 diff (휴먼명조 U+2018 정합, **목차 페이지번호 영역 변동 없음 BEFORE = AFTER**)
  - hwp3-sample16-hwp5 1 + exam_kor 17 + aift 5 (HY신명조 narrow 정합, golden issue-617 영역)
  - hwp3-sample16 / exam_math / biz_plan **diff=0** (무영향)
  - 페이지 수 회귀 부재 (모든 fixture BEFORE = AFTER)
- [x] 산출물 `output/poc/pr1026/{before,after}/` 규약 배치 (총 550 SVGs)
- [x] WASM 빌드 (Docker, **4.89MB**, PR #1034 와 동일 — 본 PR 코드 변경 작음) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (KTX 페이지번호 정합 유지 + 휴먼명조 narrow 정합 한컴 정답지 정합)
- [x] no-ff merge `3d9bf994` → devel push (`3d9bf994`)
- [x] PR #1026 close
- [x] 검토 문서 archives 이동 (v2), 임시 브랜치 (pr1026-test / pr1026-review) 정리

핵심: @HaimLee-4869 **1차 보류 PR 재제출 모범 사례**. 1차 검토 (5/20)
시 KTX 페이지번호 +10px 회귀 (`feedback_visual_judgment_authority`
작업지시자 "이게 틀린겁니다" 판정) 로 옵션 B 보류 → 작성자가 (1) base
rebase (2) **KTX 회귀 가드 영구화** `tests/issue_874_ktx_toc_page_number_right_align.rs`
(98 lines, max_x < 695.0 단언) (3) issue-267 KTX golden 보존 후
재제출. 본 환경 검증 결과 회귀 가드 통과 + svg_snapshot 8/8 보존 +
KTX 목차 페이지번호 BEFORE=AFTER 좌표 동일 → 1차 보류 사유 완전 정정.

**1차 검토 회귀 원인 재정정**: 1차 보고서 `mydocs/pr/archives/pr_1026_report.md`
의 "H2 분기 순서 변경" 추정이 잘못된 분석으로 판명. 진짜 원인은 1차
cherry-pick + UPDATE_GOLDEN 일괄 갱신 (issue-267 KTX golden 도 함께
변경) 또는 통째-파일 교체로 PR #1021 코드 유실 (본 PR 의 KTX 회귀
가드 코드 본문 명시: "stale 브랜치를 통째-파일 교체로 통합하면서 PR
#1021 코드가 유실되어 이 +10px 이탈이 재현된 사례"). 본 PR 재제출이
회귀 가드 영구화로 진짜 원인 차단.

`feedback_pr_supersede_chain` 권위 사례 — 1차 보류 → 회귀 가드 영구화
+ rebase → 재제출의 완전한 사이클. 작성자가 1차 검토 권위
(`feedback_visual_judgment_authority`) 를 받아들이고 회귀 가드로
정정하는 모범적 supersede 패턴.

`feedback_image_renderer_paths_separate` 정합 — native (`EmbeddedTextMeasurer`)
+ WASM (`WasmTextMeasurer::wasm_internals::measure_char_width_hwp`)
두 path 동시 fix (H1+H2 native + H3 WASM). 직전 PR #1021 (단일-run
RIGHT + leader) 패턴 일관.

`feedback_hancom_compat_specific_over_general` 정합 — U+2018/U+2019/
U+2027 narrow 분기 + `glyph_w >= em_size` 조건 (정상 DB 값 영향 0).
함초롬바탕/Pretendard/맑은 고딕 무영향.

본 PR 본질 (PR 본문 명시):
- 휴먼명조 U+2027 → 0.5em → 0.3em (도청 공직기강 page 1 "선제적‧적극적", "갑질‧비위" 가운뎃점 한컴 정합)
- HY중고딕/HY신명조/HY견명조 U+2018/U+2019 동일 효과
- WASM Canvas path 동기화로 rhwp-studio 시각 정합

별도 PR 권장 영역 (PR 본문 명시):
- U+00B7 ('·' Middle Dot) 본문 영역 시각 NG (Task #630 trade-off, context-aware 처리 후속)
- U+2014 / U+2016 등 다른 General Punctuation block
- HY family fallback 메트릭 보정 (별도 트랙)

보고서: `mydocs/pr/archives/pr_1026_review_v2.md`
